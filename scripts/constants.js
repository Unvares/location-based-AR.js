// global structures
const scene = document.querySelector('a-scene');
const UI = document.querySelector('.UI');

// scene elements

// UI elements
const loadingButton = UI.querySelector('.UI__button');
const controlPanel = UI.querySelector('.control-panel');
const rotationCircle = UI.querySelector('.control-panel__rotationCircle');
const positionController = UI.querySelector('.control-panel__positionController');
const scaleScroller = UI.querySelector('.control-panel__scaleScroller');